{
  "name": "domotique",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx http-server . -p 8080 -c-1",
    "posts:fix": "node tools/update_current_posts.mjs",
    "refresh:niche": "node tools/generate_niche_articles.mjs",
    "refresh:rss": "node tools/refresh_content.mjs",
    "refresh:all": "npm run posts:fix && npm run refresh:niche && npm run refresh:rss",
    "site:check": "node tools/site_check.mjs"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
{
  "scripts": {
    "site:auto-fix": "node tools/auto_fix.mjs",
    "ci:heal": "npm run site:auto-fix && npm run posts:fix && npm run site:check"
  }
}
